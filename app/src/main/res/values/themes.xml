<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema para a Splash Screen Nativa -->
    <style name="Theme.SaveGameApp.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenAnimationDuration">200</item>
        <item name="postSplashScreenTheme">@style/Theme.SaveGameApp</item>
    </style>

    <style name="Theme.SaveGameApp" parent="android:Theme.Material.Light.NoActionBar" />
</resources>